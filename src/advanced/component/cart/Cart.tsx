export const Cart = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">장바구니</h1>
      <div id="cart-items">{/* {cartList?.map((item) => CartItem(item).render)} */}</div>
    </div>
  );
};
