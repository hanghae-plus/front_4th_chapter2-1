// 고정 할인율
export const DISCOUNTS = Object.freeze({
  RATE_20: 0.2,
  RATE_10: 0.1,
  RATE_15: 0.15,
  RATE_25: 0.25,
  RATE_5: 0.05,
});

// 대량 구매
export const BULK_DISCOUNT = Object.freeze({
  THRESHOLD: 30,
  RATE: 0.25,
});

// 특별 할인
export const SPETIAL_DISCOUNT =  Object.freeze({
  LUCKY: 0.8,
  SUGGESTED: 0.95,
});
